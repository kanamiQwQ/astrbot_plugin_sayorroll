<div align="center">
  <h1>✨ astrbot-plugin-sayoroll ✨</h1>
  <p>AstrBot的随机选择(roll)与概率插件</p>
</div>

## 📖 介绍
一个替你做决定的小玩意。

本插件移植自[`nonebot-plugin-sayoroll`](https://github.com/mas-alone/nonebot-plugin-sayoroll)。

## 💿 安装

1. 将本插件的整个文件夹放入 AstrBot 的插件目录下。
2. 确保依赖正常。
3. 重启 AstrBot 即可自动加载该插件。

## 🎉 使用说明

### 1. 掷点与自动选择 (`/roll` 指令)
触发指令使用 `/roll`（如果你的 AstrBot 配置了其他指令前缀，请替换 `/`）。